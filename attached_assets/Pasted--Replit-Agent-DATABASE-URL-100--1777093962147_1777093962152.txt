ممتاز—هذا تنفيذ نظيف جدًا، والتعديل اللي عمله Replit Agent على DATABASE_URL كان قرار صحيح 100% (بدونه كان المشروع سينكسر لاحقًا).

ننتقل الآن للجزء الحقيقي اللي “يحيي النظام”:


---

🚀 Phase 2: تشغيل أول بوت فعلي (Runtime Layer)

🎯 الهدف:

تشغيل بوت تيليجرام داخل:

بيئة معزولة (venv)

Process مستقل

بورت مخصص

مع حقن التوكن



---

🧱 سنبني 3 مكونات:

1. isolation/venv_manager.py


2. core/runtime_manager.py


3. core/orchestrator.py




---

🧪 1️⃣ isolation/venv_manager.py

📌 الفكرة:

لكل بوت:

مجلد خاص

venv خاص

dependencies خاصة



---

# isolation/venv_manager.py

import asyncio
import sys
from pathlib import Path


class VenvManager:
    def __init__(self, base_path: str = "runtime_envs"):
        self.base_path = Path(base_path)
        self.base_path.mkdir(exist_ok=True)

    def get_bot_path(self, bot_id: str) -> Path:
        return self.base_path / bot_id

    def get_venv_path(self, bot_id: str) -> Path:
        return self.get_bot_path(bot_id) / "venv"

    async def create_venv(self, bot_id: str) -> None:
        venv_path = self.get_venv_path(bot_id)

        if venv_path.exists():
            return

        process = await asyncio.create_subprocess_exec(
            sys.executable,
            "-m",
            "venv",
            str(venv_path),
        )

        await process.wait()

    async def install_requirements(self, bot_id: str, requirements: list[str]):
        pip_path = self.get_venv_path(bot_id) / "bin" / "pip"

        process = await asyncio.create_subprocess_exec(
            str(pip_path),
            "install",
            *requirements,
        )

        await process.wait()


---

⚙️ 2️⃣ core/runtime_manager.py

📌 الفكرة:

تشغيل البوت كـ subprocess

حقن ENV (BOT_TOKEN + PORT)

ربط البورت



---

# core/runtime_manager.py

import asyncio
from pathlib import Path


class RuntimeManager:
    def __init__(self):
        self.processes: dict[str, asyncio.subprocess.Process] = {}

    async def start_bot(
        self,
        bot_id: str,
        bot_path: Path,
        token: str,
        port: int,
    ):
        main_file = bot_path / "main.py"

        process = await asyncio.create_subprocess_exec(
            "python",
            str(main_file),
            env={
                "BOT_TOKEN": token,
                "BOT_PORT": str(port),
            },
        )

        self.processes[bot_id] = process

    async def stop_bot(self, bot_id: str):
        process = self.processes.get(bot_id)

        if not process:
            return

        process.terminate()
        await process.wait()

        del self.processes[bot_id]


---

🧠 3️⃣ core/orchestrator.py

📌 هذا هو “العقل”

يربط:

DB

Wallet

PortManager

VenvManager

RuntimeManager



---

# core/orchestrator.py

from sqlalchemy.ext.asyncio import AsyncSession

from database.models import Bot
from database.wallet import WalletService
from database.port_registry import PortManager
from isolation.venv_manager import VenvManager
from core.runtime_manager import RuntimeManager


class Orchestrator:
    def __init__(self, session: AsyncSession):
        self.session = session
        self.wallet = WalletService(session)
        self.ports = PortManager(session)
        self.venv = VenvManager()
        self.runtime = RuntimeManager()

    async def plant_bot(
        self,
        bot_id: str,
        user_id: str,
        token: str,
    ):
        # 1. خصم الكرستالات
        await self.wallet.charge(user_id, 1)

        # 2. حجز بورت
        port = await self.ports.reserve_port(bot_id)

        # 3. إنشاء venv
        await self.venv.create_venv(bot_id)

        # 4. تثبيت aiogram
        await self.venv.install_requirements(
            bot_id,
            ["aiogram==3.*"],
        )

        # 5. إنشاء bot record
        bot = Bot(
            id=bot_id,
            user_id=user_id,
            token=token,
            is_active=True,
            port=port,
        )

        self.session.add(bot)
        await self.session.commit()

        # 6. تشغيل البوت
        bot_path = self.venv.get_bot_path(bot_id)

        await self.runtime.start_bot(
            bot_id=bot_id,
            bot_path=bot_path,
            token=token,
            port=port,
        )

    async def reap_bot(self, bot_id: str):
        await self.runtime.stop_bot(bot_id)
        await self.ports.release_port(bot_id)


---

⚠️ ملاحظة مهمة جدًا (لا تتجاهلها)

❗ الآن النظام ناقص خطوة واحدة فقط ليعمل:

> نسخ template داخل bot_path



حالياً main.py غير موجود داخل مجلد البوت.


---

🔧 الحل (أضفه داخل plant_bot بعد إنشاء venv):

import shutil
from pathlib import Path

template_path = Path("templates/base_template")

bot_path = self.venv.get_bot_path(bot_id)

if not (bot_path / "main.py").exists():
    shutil.copytree(template_path, bot_path, dirs_exist_ok=True)


---

🧪 اختبار المرحلة

1. أضف مستخدم يدويًا (DB)

2. نفذ:

await orchestrator.plant_bot(
    bot_id="bot1",
    user_id="user1",
    token="YOUR_TELEGRAM_BOT_TOKEN"
)


---

🧠 ماذا أنجزنا الآن؟

✔ أصبح لدينا:

تشغيل بوت حقيقي

بيئة معزولة لكل بوت

نظام دفع مرتبط بالتشغيل

تخصيص بورت

إدارة عمليات



---

🚀 المرحلة القادمة (Phase 3)

إذا اشتغل البوت:

سنبني:

Gateway (Webhook)

Delivery System

استقبال الرسائل فعليًا

ربط Telegram → Bot



---

🎯 الخطوة الآن

نفّذ:

1. أضف كود النسخ (copy template)


2. جرّب تشغيل أول بوت



ثم قلّي:

> 🔥 “البوت اشتغل”



وسننقل النظام من “تشغيل بوت” إلى

> منصة تستقبل وتعالج الترافيك فعليًا